# دليل الرفع بالترتيب

الترتيب مقصود: كل خطوة تعتمد على ما قبلها. لا تقفز.

---

# المرحلة صفر — الأمان (مرة واحدة، قبل كل شيء)

مفتاح Firebase الحالي دخل تاريخ git ووصل GitHub. **أبطله قبل أي شيء آخر.**

1. Firebase Console ← ⚙️ **Project Settings** ← **Service accounts**
2. **Generate new private key** ← ينزل ملف JSON
3. Google Cloud Console ← **IAM & Admin** ← **Service Accounts** ← اضغط الحساب ← تبويب **Keys** ← احذف المفتاح القديم `1ad42053…`
4. انقل الملف الجديد خارج المشروع:
   ```bash
   mkdir -p ~/keys && mv ~/Downloads/o2bot-*.json ~/keys/o2bot-key.json
   ```

**لا تضع الملف داخل مجلد المشروع أبداً.** مكانه الوحيد متغيّر البيئة.

تحقق أنه يعمل:

```bash
cd ~/Documents/o2bot
node test-firebase.js ~/keys/o2bot-key.json
```

يجب أن ترى `✅ المفتاح يعمل` مع عدد الأصناف والحسابات.

---

# المرحلة ١ — الصور على Firebase Hosting

الصور مستقلة عن البوت والموقع، وأخفّها تغييراً. ابدأ بها.

```bash
cd ~/Documents/o2bot
firebase login
firebase use --add          # اختر o2bot-b7a51
firebase deploy --only hosting
```

يطبع في النهاية:

```
Hosting URL: https://o2bot-b7a51.web.app
```

**تحقق قبل المتابعة:** افتح `https://o2bot-b7a51.web.app` في المتصفح — يجب أن ترى شبكة فيها 109 صورة. إن رأيت 404 فالنشر لم يشمل `public/index.html`.

---

# المرحلة ٢ — البوت على Render

## ٢.١ فحص قبل الرفع

```bash
cd ~/Documents/o2bot
npm install
bash preflight.sh
```

يفحص: الملفات · سلامة الكود · المنيو ومسارات الصور · **الأسرار في git** · تجربة إقلاع محلية.

**لا ترفع قبل أن يقول `✅ جاهز للرفع`.** إن أبلغ عن مفتاح في التاريخ فارجع للمرحلة صفر.

## ٢.٢ الرفع إلى GitHub

```bash
git add -A
git commit -m "تحديث البوت"
git push
```

رفضه GitHub بسبب سرّ؟ **لا تضغط رابط unblock-secret**. نظّف:

```bash
git reset --soft origin/main
git rm --cached "<اسم-ملف-المفتاح>"
mv "<اسم-ملف-المفتاح>" ~/keys/
git add -A && git commit -m "تحديث البوت" && git push
```

## ٢.٣ متغيرات البيئة على Render

Render ← خدمتك ← **Environment**:

| المتغيّر | القيمة | إلزامي |
|---|---|---|
| `FIREBASE_SERVICE_ACCOUNT` | محتوى `~/keys/o2bot-key.json` كاملاً من `{` إلى `}` | ✅ |
| `SESSION_SECRET` | نص عشوائي طويل (30 حرفاً فأكثر) | ✅ |
| `NODE_ENV` | `production` | مستحسن |
| `RENDER_EXTERNAL_URL` | رابط خدمتك — **يمنع النوم وانقطاعات 408** | مهم |
| `RESET_ADMIN_PASSWORD` | كلمة مرور جديدة — **احذفه بعد أول دخول** | عند الحاجة |

لا تضع اقتباسات حول محتوى JSON، ولا تحذف شيئاً من `private_key`.

## ٢.٤ راقب السجل

Render ← **Logs**. الأسطر التي تؤكد النجاح بالترتيب:

```
🔑 مفتاح Firebase: firebase-adminsdk-…@o2bot-b7a51.iam.gserviceaccount.com
   المشروع: o2bot-b7a51
✅ السيرفر شغال
✅ Firebase: … صنف محمّل
🖼️  نطاق الصور ضُبط تلقائياً: https://o2bot-b7a51.web.app
👥 الحسابات: murad(سوبر أدمن)، …
⏰ نداء ذاتي كل 12 دقيقة
   ⏱️  فتح الاتصال بخوادم واتساب
```

سطر `❌❌❌ تعذّر تحميل البيانات` يعني أن المفتاح خاطئ — **ارجع للمرحلة صفر**. لا تكمل: البوت لن يعمل والصور لن تظهر والمنيو لن يتحدّث. كلها من هذا السبب.

## ٢.٥ الربط بواتساب

افتح `https://<خدمتك>.onrender.com/link` ← سجّل الدخول ← اختر **كود من 8 خانات** أو **QR**.

شريط المراحل يريك أين وصلت. إن علق أكثر من دقيقتين راجع السجل.

## ٢.٦ الفحص الشامل

```bash
node doctor.js https://<خدمتك>.onrender.com murad <كلمة_المرور>
```

يجب أن تكون المستويات الستة ✅ قبل الانتقال للموقع.

## ٢.٧ أغلق الباب

1. اللوحة ← **الحسابات** ← غيّر كلمات المرور الأربع
2. Render ← Environment ← **احذف `RESET_ADMIN_PASSWORD`** إن كنت ضبطته
3. اللوحة ← الإعدادات ← **سلوك البوت**: أطفئ «يتطلب كلمة تفعيل» إن كان الرقم للمطعم

---

# المرحلة ٣ — موقع الزبائن

**لا تبدأ قبل أن يعمل البوت**، لأن الموقع يقرأ منه.

## ٣.١ تحقق أن نقطة البوت تعمل

افتح في المتصفح:

```
https://<خدمتك>.onrender.com/api/public/menu?branch=gaza
```

يجب أن ترى `{"ok":true,"counts":{"total":116,...}}`. إن لم تظهر فالمشكلة في البوت لا الموقع.

## ٣.٢ متغيرات البيئة على منصة الموقع

Vercel أو Netlify ← **Environment Variables**:

```
BOT_ORIGIN=https://<خدمتك>.onrender.com
NEXT_PUBLIC_USE_BOT_PROXY=1
```

## ⚠️ ٣.٣ أعد النشر — الخطوة الأكثر إغفالاً

**متغيرات `NEXT_PUBLIC_` تُدمج في الكود وقت البناء لا وقت التشغيل.**

إضافتها بعد آخر بناء لا تفعل شيئاً. يجب **Redeploy** بعد إضافتها، وإلا بقي الموقع يعمل بالمنيو الثابت ولن يتغيّر شيء عند إغلاق صنف.

Vercel: Deployments ← آخر نشر ← ⋯ ← **Redeploy**
Netlify: Deploys ← **Trigger deploy** ← **Clear cache and deploy site**

## ٣.٤ تحقق من الربط

افتح `https://<موقعك>/live-debug` — صفحة تشخيص تفحص أربعة مستويات وتقول أين الخلل بالضبط:

- إعدادات البناء (هل وصل الرابط؟)
- الاتصال بالبوت (كم صنفاً وصل؟)
- الدمج (كم اختفى فعلياً؟)
- صور الموقع

## ٣.٥ الاختبار النهائي

1. اللوحة ← المنيو ← أغلق صنفاً معروفاً مثل «باشكا»
2. انتظر دقيقة
3. حدّث صفحة القسم في الموقع ← يجب أن يختفي «باشكا»
4. أعد تفعيله ← يظهر

---

# مسائل شائعة

| العرض | السبب الأرجح | الحل |
|---|---|---|
| كل شيء معطّل معاً | Firebase لا يُحمَّل | `node test-firebase.js` |
| صور المنيو في **اللوحة** لا تظهر | نطاق الصور فارغ | يُضبط تلقائياً؛ أو اضغط زر الشريط الأصفر |
| صور المنيو في **الموقع** لا تظهر | `public/menu` لم يُرفع | تأكد أنه ليس في `.gitignore`، وأن المنصة لم تتخطّه لحجمه |
| الإغلاق لا يظهر في الموقع | لم تُعِد النشر بعد ضبط المتغيرات | Redeploy |
| البوت لا يرد | أرسلت من الرقم المربوط نفسه | اختبر من هاتف آخر · وانظر بطاقة «لماذا لم يرد البوت؟» |
| انقطاعات 408 | الخدمة تنام | اضبط `RENDER_EXTERNAL_URL` |
| رمز 440 | الرقم مرتبط بجلسة أخرى | أوقف النسخة الثانية |
| رمز 401 | مضت 14 يوماً على الهاتف | افتح واتساب على الهاتف ثم أعد الربط |

---

# ملخص الترتيب

```
0) أبطل المفتاح القديم وأنشئ جديداً        ← أمان
1) firebase deploy --only hosting          ← الصور
2) preflight → push → env → logs → /link   ← البوت
3) env → REDEPLOY → /live-debug            ← الموقع
```

المرحلة ٣ تعتمد على ٢، والمرحلة ٢ تعتمد على ٠. لا تقفز.
