# صور المنيو — الرفع والاستدعاء

138 صورة مضغوطة في `public/menu/`، بنفس المسارات المكتوبة في `menu-source.js` تماماً. بعد الرفع تكفي خطوة واحدة في اللوحة لتظهر كلها.

الأصل 40 ميغابايت، وبعد الضغط **7.8 ميغابايت** — توفير 80% بلا فرق مرئي (عرض أقصى 900px، جودة 80).

---

## ⚠️ اقرأ هذا أولاً

**Firebase Storage لم يعد مجانياً بلا بطاقة بنكية.** منذ 3 فبراير 2026 صار يتطلب خطة **Blaze** حتى لو بقي استهلاكك صفراً ضمن الحد المجاني. المشاريع على Spark فقدت الوصول لحاوياتها.

لذلك الطريقة الموصى بها هنا هي **Firebase Hosting** — مجاني على Spark بلا بطاقة، بحدود 10 غيغابايت تخزين و360 ميغابايت نقل يومياً. صورك 7.8 ميغابايت، أي 0.08% من الحد.

كلا الطريقتين مشروحتان تحت.

---

# ✅ الطريقة الأولى: Firebase Hosting (مجانية، موصى بها)

## 1. ثبّت أداة Firebase

```bash
npm install -g firebase-tools
firebase login
```

## 2. من داخل هذا المجلد

```bash
firebase use o2bot-b7a51     # أو: firebase init hosting
firebase deploy --only hosting
```

عند انتهاء الرفع يطبع رابطاً مثل:

```
Hosting URL: https://o2bot-b7a51.web.app
```

## 3. في اللوحة

الإعدادات العامة ← **نطاق صور المنيو** ← الصق الرابط:

```
https://o2bot-b7a51.web.app
```

احفظ. الصور الـ138 تظهر فوراً في كل البطاقات، لأن المسارات في المنيو (`/menu/shawarma/48.jpg`) تطابق بنية المجلد حرفاً بحرف.

**للتجربة قبل الحفظ:** افتح `https://o2bot-b7a51.web.app/menu/shawarma/48.jpg` في المتصفح — يجب أن تظهر صورة الفرشوحة.

---

# الطريقة الثانية: Firebase Storage (تتطلب Blaze)

إن كان مشروعك على Blaze أصلاً:

## 1. أنشئ الحاوية

Firebase Console ← Build ← **Storage** ← Get started ← اختر منطقة.

## 2. ارفع

```bash
npm install firebase-admin          # إن لم تكن مثبّتة
node upload-to-storage.js ~/keys/o2bot-key.json
```

أو بمتغيّر البيئة:

```bash
export FIREBASE_SERVICE_ACCOUNT="$(cat ~/keys/o2bot-key.json)"
node upload-to-storage.js
```

السكربت يرفع الصور محافظاً على المسارات، يجعلها عامة، ويطبع في النهاية الرابط الذي تضعه في **نطاق صور المنيو**:

```
https://storage.googleapis.com/o2bot-b7a51.firebasestorage.app
```

إن كان اسم الحاوية مختلفاً اضبط `FIREBASE_STORAGE_BUCKET` قبل التشغيل.

---

# بدائل مجانية أخرى

| الخدمة | مجاني بلا بطاقة | ملاحظة |
|---|---|---|
| **Firebase Hosting** | ✅ | الأنسب — نفس المشروع ونفس المسارات |
| موقعكم الحالي | ✅ | إن كان منشوراً، ضع نطاقه مباشرة ولا ترفع شيئاً |
| GitHub + jsDelivr | ✅ | ارفع `public/menu` لمستودع عام، النطاق: `https://cdn.jsdelivr.net/gh/USER/REPO@main` |
| Cloudinary | ✅ | 25 غيغابايت نقل شهرياً، لكن مسارات مختلفة تحتاج تعديل المنيو |

**لاحظ:** إن كان موقع المطعم منشوراً فعلاً، فالصور موجودة عليه بنفس المسارات — ضع نطاقه في «نطاق صور المنيو» وانتهِ دون رفع أي شيء.

---

# كيف يعمل الاستدعاء

اللوحة تبني رابط كل صورة هكذا:

```
نطاق صور المنيو  +  قيمة image في الصنف
https://o2bot-b7a51.web.app  +  /menu/shawarma/48.jpg
```

- **الروابط المطلقة** (`https://…`) في حقل صورة أي صنف تُستخدم كما هي وتتجاوز النطاق.
- **الحقل فارغ** ← يظهر رمز القسم بلونه.
- **الصورة معطوبة** ← تسقط تلقائياً إلى رمز القسم، فلا تظهر بطاقة مكسورة للموظف.

---

# ملاحظات

**29 صورة غير مستخدمة** في المجلد لا يشير إليها أي صنف — منها `about.jpeg` و`o2_delivery_icon.ico` وبدائل مثل `bar/57.jpg`. تُركت كما هي؛ استخدمها عند إضافة أصناف، أو احذفها لتقليل الحجم.

**التخزين المؤقت:** الصور تُرسل بترويسة `max-age=31536000, immutable`، فالمتصفح يحمّلها مرة واحدة فقط. إن غيّرت صورة باسمها نفسه غيّر الاسم أيضاً (مثلاً `48-v2.jpg`) وإلا بقيت القديمة في ذاكرة المتصفح.

**النقل اليومي:** حد Hosting المجاني 360 ميغابايت يومياً. بطاقات المنيو الـ238 مع الصور ≈ 8 ميغابايت للتحميل الأول لكل موظف، ثم صفر بفضل التخزين المؤقت. أي عشرات المرات ضمن الحد.
