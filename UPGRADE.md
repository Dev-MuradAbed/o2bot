# تحديث v3.1 — الحسابات والصلاحيات وتوفّر الأصناف

هذا التحديث يبني فوق المشروع القائم. منطق Baileys وقاموس الـ aliases ونظام التعلّم والديلفري لم يتغيّر شيء منها.

---

## ⚠️ إجراء أمني مطلوب منك أولاً

الأرشيف السابق كان يحتوي `o2bot-b7a51-firebase-adminsdk-fbsvc-*.json` — مفتاح خدمة Firebase بصلاحية admin كاملة على مشروعك. حُذف من هذه النسخة، وأُضيف نمطه إلى `.gitignore`.

**افعل الآن:**

1. Google Cloud Console ← **IAM & Admin** ← **Service Accounts**
2. اختر `firebase-adminsdk-fbsvc@o2bot-b7a51.iam.gserviceaccount.com` ← تبويب **Keys**
3. احذف المفتاح القديم ← **Add Key** ← **Create new key** ← JSON
4. ضع محتوى الملف الجديد في متغير البيئة `FIREBASE_SERVICE_ACCOUNT` على Render فقط — لا تضعه في المجلد أبداً

لاحظ أيضاً أن ملف الاستثناء كان اسمه `gitignore` بدون نقطة، أي أن Git كان يتجاهله ولا يطبّقه. صار الآن `.gitignore`.

---

## ما الجديد

### 1. أربعة حسابات بأربع كلمات مرور

تُنشأ تلقائياً عند أول تشغيل بعد التحديث. تُحفظ في Firestore ضمن نفس مستند `state`.

| المستخدم | كلمة المرور | الدور | ما يراه |
|---|---|---|---|
| `murad` | `O2-admin-2026` | سوبر أدمن — **مراد عابد** | كل الصفحات |
| `cashier` | `O2-cashier` | كاشير | المنيو + تفعيل/إغلاق فقط |
| `callcenter` | `O2-call` | كول سنتر | المنيو + تفعيل/إغلاق فقط |
| `care` | `O2-care` | كاستمر كير | المنيو + تفعيل/إغلاق فقط |

كلمات المرور مشفّرة بـ scrypt مع salt لكل حساب. **غيّرها من صفحة «الحسابات» قبل الاستخدام الفعلي** — يظهر تنبيه للسوبر أدمن ما دام أي حساب على كلمة المرور الافتراضية.

لتغيير كلمة مرور السوبر أدمن عند أول تشغيل، اضبط `DEFAULT_ADMIN_PASSWORD` في متغيرات البيئة.

### 2. الحماية على مستوى الـ API لا الواجهة

إخفاء الأزرار وحده لا يحمي شيئاً — أي شخص يفتح Console ويستدعي `fetch` مباشرة. لذلك كل نقطة `/api/*` تمر بفحص صلاحية قبل التنفيذ، وترجع 401 لغير المسجّل و403 لمن لا يملك الصلاحية.

الفحص يميّز حسب الحقل المُعدَّل لا المسار فقط:

- `PUT /api/items/45` بجسم `{"active":false}` → يحتاج `menu.toggle` ← الكاشير مسموح
- `PUT /api/items/45` بجسم `{"price":99}` → يحتاج `menu.edit` ← الكاشير ممنوع

### 3. كل تغيير يحمل اسم صاحبه

كل صنف صار يحمل `updatedBy` و`updatedRole` و`updatedAt`، ويظهر ذلك في صفحة المنيو تحت اسم الصنف.

بالإضافة لسجل مستقل في صفحة **سجل التغييرات**: الوقت، الموظف، الإجراء، التفاصيل، والمصدر (اللوحة أم واتساب). مع إحصاء آخر ٢٤ ساعة وأكثر الموظفين نشاطاً، وبحث فوري. يحتفظ بآخر 1500 عملية.

### 4. كلمة تفعيل البوت

إعداد جديد `requireTrigger` **مفعّل افتراضياً**. البوت صامت تماماً حتى يرسل الزبون إحدى كلمات التفعيل: `bot` أو `بوت` أو `o2` أو `منيو` أو `menu`. هذا يجعل ربط البوت برقم واتساب شخصي آمناً — لن يرد على محادثاتك العادية.

تنتهي حالة التفعيل بعد `triggerTimeoutMins` (20 دقيقة افتراضياً) من الخمول، أو عند إرسال `خروج`.

لإطفائه والعودة للسلوك القديم (الرد على كل رسالة، مناسب لرقم مطعم مخصص): اضبط `requireTrigger: false` من الإعدادات.

### 5. وضع تصفّح المنيو

الأرقام مخصّصة للأقسام فقط، والدورة مغلقة: كل قسم ينتهي بقائمة الأقسام من جديد + خيار الإنهاء.

```
الزبون: bot   → ترحيب + كل الأقسام مرقّمة + 0️⃣ إنهاء العملية
الزبون: 6     → كل أصناف الحلويات بأسعار مصطفّة
                ثم: «بدك تشوف قسم تاني؟» + الأقسام + 0️⃣ إنهاء
الزبون: 1     → أصناف الشاورما + الأقسام من جديد
الزبون: 0     → «تم إنهاء العملية» ويعود البوت صامتاً
```

الأسعار تصطفّ بنقاط تُحسب من أطول اسم في القسم، فتبدو القائمة كبطاقة منيو جاهزة للإرسال للزبون:

```
🍰 الحلويات   (14 صنف)
━━━━━━━━━━━━━━━
كنافة نوتيلا ..... 15 ₪
كنافة دبي ........ 20 ₪
بان كيك نوتيلا ... 25 ₪
كنافة نابلسية .... 30 ₪
━━━━━━━━━━━━━━━
```

الأصناف المغلقة لا تظهر إطلاقاً، وعدد الأصناف بجانب اسم القسم يتحدّث تلقائياً. القسم الذي كل أصنافه مغلقة يُكتب بجانبه "(مغلق حالياً)".

الطلب ما زال متاحاً بكتابة اسم الصنف ("بدي كنافة دبي") — الأرقام للتنقّل فقط حتى لا يختلط الأمر على الزبون.

**إصلاح مرافق:** كانت `detectCategoryQuery` تتجاهل الأصناف المغلقة، فاسم صنف مغلق مثل "مولتن كيك" يسقط على مطابقة القسم فيُعرض المنيو بدل رسالة "غير متوفر". صار الفحص يشمل كل الأصناف، فالرد الآن اعتذار + بدائل من نفس القسم.

### 6. أوامر الطاقم من واتساب

الكاشير أثناء الضغط لا يفتح لوحات تحكم، لكنه يرسل رسالة واتساب. كل أمر يُسجَّل باسم صاحبه مع `source: whatsapp`.

| الأمر | ما يفعله |
|---|---|
| `#دخول cashier O2-cashier` | ربط رقم الموظف بحسابه (مرة واحدة) |
| `#اغلاق كنافة دبي` | إغلاق صنف فوراً |
| `#تفعيل كنافة دبي` | إعادة تفعيله |
| `#اغلاق حلويات` | إغلاق قسم كامل |
| `#المغلق` | كل الأصناف المغلقة ومن أغلقها |
| `#خروج_طاقم` | فك ربط الرقم |

عند تغيّر توفّر أي صنف يُرسل إشعار لمجموعة الطاقم (إن كان `groupId` مضبوطاً) يوضّح الصنف ومن غيّره ومتى.

### 7. الربط بكود بدل QR

صفحة جديدة **`/link`** (رابط «🔗 ربط واتساب» في الشريط الجانبي) تتيح الربط بكود من 8 خانات بدل مسح الرمز بالكاميرا.

```
افتح /link ← اختر «🔢 كود من 8 خانات»
أدخل رقم البوت بصيغة دولية: 970567743979
اضغط «توليد كود الربط» ← يظهر مثلاً: TA65-DJMT

على الهاتف:
واتساب ← الإعدادات ← الأجهزة المرتبطة ← ربط جهاز
← «الربط برقم الهاتف بدلاً من ذلك» ← أدخل الكود
```

مفيد حين لا كاميرا في الهاتف، أو حين تربط من جهاز بعيد ولا شاشة أمامك لتصوّرها.

**صيغة الرقم:** بالأرقام فقط مع رمز الدولة، بلا `+` ولا مسافات ولا شرطات. الواجهة تنظّفه تلقائياً، فلو كتبت `+970 56-774-3979` يُرسل `970567743979`.

**تفاصيل التنفيذ التي تهم:**

- الاستدعاء لا يتم فور إنشاء السوكيت بل عند أول حدث `qr` — هذا الحدث يُطلق حتى في وضع الكود، وتوثيق Baileys ينصّ على استخدامه كمُحفِّز لأن السوكيت قد لا يكون جاهزاً قبله. استدعاؤه مبكراً هو الخطأ الشائع الذي يجعل الكود لا يعمل.
- الكود لا يُطلب إلا لجلسة **غير مسجّلة**، لذا التبديل يمسح `baileys_auth` أولاً. لا يمكن توليد كود لجلسة مرتبطة أصلاً.
- طلب واحد فقط لكل محاولة اتصال. واتساب يعيد إرسال حدث `qr` كل ~20 ثانية، ولو طلبنا كوداً مع كل حدث لعاد الخطأ `429 rate-overlimit` وفشل الربط قبل أن تُدخل الكود.
- إن فشل التوليد لأي سبب يرجع النظام تلقائياً إلى QR بدل أن يتوقف.
- الكود ينتهي بعد 3 دقائق فتطلب الواجهة توليد غيره.

**قيد من واتساب نفسه:** طريقة الكود تربط **جهازاً واحداً فقط لكل رقم**. رمز QR لا يخضع لهذا القيد.

زر **«فك الربط ومسح الجلسة»** في نفس الصفحة يمسح `baileys_auth` ويعيد التشغيل — تحتاجه قبل ربط رقم مختلف.

### 8. اللوحة على كل الأجهزة

اللوحة كانت بلا أي `@media` وشريط جانبي ثابت بعرض 210px — أي غير قابلة للاستخدام على الجوال.

| العرض | ما يتغيّر |
|---|---|
| أقل من 640px | عمود واحد، بطاقات مكدّسة، خط الإدخال 16px لمنع تكبير iOS |
| أقل من 760px | الجداول تتحوّل إلى بطاقات كل حقل بعنوانه |
| أقل من 900px | الشريط الجانبي ينزلق من زر ☰ مع خلفية معتمة وإغلاق بـ Escape |
| أكثر من 920px | صفوف المنيو في عمودين |
| أكثر من 1400px | ثلاثة أعمدة |

مع إخفاء عناصر التنقّل حسب صلاحية الحساب، وتكبير مفاتيح التفعيل على الشاشات اللمسية، وإعادة توجيه تلقائية لصفحة الدخول عند أي رد 401.

---

## متغيرات البيئة الجديدة

| المتغير | الافتراضي | الوصف |
|---|---|---|
| `SESSION_SECRET` | — | **اضبطه على Render**. سر توقيع جلسات اللوحة. تغييره يُخرج الجميع |
| `SESSION_TTL_HOURS` | `12` | مدة صلاحية الجلسة |
| `DEFAULT_ADMIN_PASSWORD` | `O2-admin-2026` | كلمة مرور السوبر أدمن عند أول تشغيل فقط |
| `O2_TEST_MODE` | — | `1` لتشغيل المشروع بلا Firebase (تجربة محلية) |

على Render: **Environment** ← Add ← `SESSION_SECRET` بقيمة عشوائية طويلة.

---

## نقاط API المضافة

```
POST /api/auth/login      { username, password }  → يضبط كوكي الجلسة
GET  /api/auth/me                                 → الحساب الحالي وصلاحياته
POST /api/auth/logout
GET  /api/users                                   → قائمة الحسابات (سوبر أدمن)
PUT  /api/users/:id       { displayName, username, whatsappNumber, password }
GET  /api/audit                                   → السجل والإحصاءات
POST /api/cats/toggle     { cat, active }         → تفعيل/إغلاق قسم كامل
```

المسارات القديمة كلها كما هي، أضيف لها فحص الصلاحية فقط.

---

## التجربة المحلية بلا Firebase

```bash
npm install
O2_TEST_MODE=1 SESSION_SECRET=test node index.js
```

ثم `http://localhost:3000` ← دخول بـ `murad` / `O2-admin-2026`. البيانات في الذاكرة فقط وتنمحي عند الإيقاف.

---

## ملفات تغيّرت

```
auth.js            جديد — الأدوار والصلاحيات وكلمات المرور والسجل
index.js           مُرقَّع — بوابة التفعيل، التصفّح بالأرقام، أوامر الطاقم،
                   صفحة الدخول، حارس الـ API، ختم من غيّر على كل صنف
dashboard.html     مُرقَّع — طبقة استجابة، شريط جانبي منزلق، بطاقة المستخدم،
                   صفحتا الحسابات والسجل، إخفاء التنقّل حسب الدور
.gitignore         جديد (كان gitignore بلا نقطة = غير فعّال)
```

---

## ما لم يُنجز بعد

- جدولة تلقائية: إغلاق أصناف الإفطار بعد الظهر وفتحها فجراً دون تدخل
- تقرير أسبوعي بأكثر الأصناف نفاداً — البيانات موجودة في السجل بالفعل
- إشعار للكاشير عند نفاد صنف موجود في طلب قيد التحضير

---

# تحديث v4.0

## 🐛 إصلاح حرج: عودة الأصناف مُفعّلة وحدها

كان `loadState` يقارن عدد الأصناف المحفوظة بعدد الأصناف المكتوبة في الكود:

```js
const CODE_COUNT = STATE.items.length;          // 69
if (saved.items.length >= CODE_COUNT) STATE.items = saved.items;
else { /* تُهمل القائمة المحفوظة كلها */ }
```

بمجرد حذف صنف واحد يصبح المحفوظ 68 < 69، فتُرمى القائمة كاملة وتُستخدم نسخة الكود التي كل أصنافها `active: true`. النتيجة: كل حالات الإغلاق تضيع، والصنف المحذوف يعود.

أعدت إنتاج الخلل باختبار يحاكي إعادة التشغيل، ثم أصلحته:

- **المحفوظ هو المرجع دائماً.** لا مقارنة أعداد إطلاقاً.
- **شواهد حذف (`deletedItemIds`)** تمنع عودة صنف حذفه المستخدم، مع إبقاء إمكانية إضافة أصناف جديدة من الكود مستقبلاً.
- **حفظ فوري للتغييرات الحرجة.** كان `saveState()` يؤجّل الكتابة 3 ثوانٍ؛ إيقاف الخدمة خلالها يبتلع التغيير. صار تبديل التوفّر والحذف وتبديل القسم — من اللوحة أو من واتساب — ينتظر `saveStateNow()` قبل الرد.

## وضع التصفّح فقط (`browseOnly`)

إعداد جديد مفعّل افتراضياً. البوت لا يفعل شيئاً سوى عرض الأقسام والأصناف المتوفرة:

- قبل كلمة التفعيل: **صمت تام** — لا رد على تحية ولا سؤال ولا اسم صنف.
- بعد `bot` أو `منيو`: الأرقام تعرض الأقسام، `0` ينهي.
- أي رسالة أخرى تعيد عرض الأقسام بدل محاولة فهمها.

لا ترحيب، لا أسعار مفردة، لا طلبات، لا توصيل، لا أسئلة عامة. لإعادة السلوك الكامل: `browseOnly = false`.

## إدارة المنيو ببطاقات

الجدول النصي استُبدل بشبكة بطاقات: صورة الصنف (أو رمز القسم بلونه إن لم توجد صورة)، الاسم، السعر، القسم، ومن غيّر التوفّر ومتى، وزر عريض **تفعيل / إلغاء التفعيل**.

البطاقة المغلقة تتحوّل للرمادي بإطار أحمر وشارة «مغلق» واسم مشطوب — تُقرأ الحالة بلمحة دون قراءة نص.

أُضيف حقل **رابط صورة** في نافذة تعديل الصنف. اتركه فارغاً ليظهر رمز القسم بلونه، والصورة المعطوبة ترجع للرمز تلقائياً.

الشبكة تتكيّف: عمودان على الجوال، وتزيد الأعمدة مع اتساع الشاشة.

## بطاقات الحسابات

الاستمارات المتراصّة تحت بعضها استُبدلت بشبكة بطاقات، وأُضيف:

- **➕ حساب جديد** — اسم ظاهر، اسم مستخدم، دور، كلمة مرور.
- **🗑️ حذف حساب** بحمايتين: لا تحذف حسابك الحالي، ولا آخر سوبر أدمن.
- **تغيير الدور** من قائمة منسدلة داخل البطاقة (معطّلة على حسابك أنت).

بطاقتك تُميَّز بإطار أخضر وكلمة «أنت». التحقق يتم في الخادم لا الواجهة: الكاشير يحصل على 403 لو استدعى `POST /api/users` مباشرة.

نقاط جديدة: `POST /api/users` · `DELETE /api/users/:id` · `PUT /api/users/:id` يقبل `role`.

## v4.1 — إعادة تصميم شاشة الحسابات

التصميم السابق كان يضع استمارة كاملة بستة حقول داخل كل بطاقة. ارتفاع البطاقة الواحدة ~400px، فتسعة حسابات = تمرير بطول 3600px على الجوال — لهذا لم تكن تظهر كلها.

**المبدأ الجديد: نظرة عامة أولاً، والتعديل في نافذة.**

- **صف مدمج لكل حساب** (~75px): الحرف الأول بلون الدور، الاسم، `@اسم_المستخدم`، ووسوم سريعة — الدور، «كلمة مرور افتراضية» إن وُجدت، «📱 مربوط» إن رُبط رقم واتساب، وآخر دخول. تسعة حسابات تظهر دفعة واحدة.
- **شرائح الأدوار** أعلى القائمة مع عدد كل دور، تضغطها لتصفية القائمة.
- **بحث فوري** بالاسم أو اسم المستخدم.
- **نافذة واحدة للإضافة والتعديل**، تعرض وصف صلاحيات الدور المختار قبل الحفظ.
- حسابك مميّز بشريط أخضر وكلمة «أنت»، وزر حذفه معطّل.

**تحسين تقني:** استُبدلت `onclick` المُدمجة داخل HTML بتفويض أحداث عبر `data-act`. الاسم الذي يحوي اقتباساً مفرداً (مثل `O'Brien`) كان يكسر السلسلة النصية؛ الآن لا نصوص مُركّبة أصلاً.

اختُبر بتسعة حسابات: الصفوف التسعة تُرسم كاملة، الشرائح تعدّ صحيحاً (كاشير 4، كول سنتر 2، كاستمر كير 2، سوبر أدمن 1)، البحث والفلترة يعملان، ورفض تنزيل آخر سوبر أدمن وحذف الحساب الحالي ما زال قائماً على الخادم.

## v4.2 — إصلاح بنيوي في شاشة الحسابات

عند إضافة صفحتَي «الحسابات» و«سجل التغييرات» في v3.1، أُدرجتا بالخطأ **داخل عنصر التوست** `<div class="toast" id="toast">` بدل `.main`.

نتيجتان خطيرتان:

1. `.toast` عنصر `position:fixed; bottom:22px; transform:translateY(80px); white-space:nowrap`. أي أن صفحة الحسابات كانت مثبّتة أسفل الشاشة ومدفوعة 80px للأسفل — لهذا كانت البطاقات مقصوصة ولا تظهر كاملة.
2. دالة `toast()` تنفّذ `t.textContent = msg`، وهذا **يمسح كل أطفال العنصر**. أي أن أول رسالة تنبيه كانت تمحو صفحتَي الحسابات والسجل من الصفحة نهائياً حتى إعادة التحميل.

**الإصلاح:** نُقلت الصفحتان إلى `.main` مع باقي الصفحات، ونُقلت نافذة الحساب بجانب باقي النوافذ، وعاد التوست عنصراً فارغاً `<div class="toast" id="toast"></div>`.

اختُبر: التوست لم يعد يحوي أطفالاً، والصفوف التسعة تصمد بعد استدعاء `toast()`، ووسوم `<div>` متوازنة (528/528).

### خطأ مرافق: نافذة السائق ظاهرة دائماً

`#modal-driver` كان يستخدم `class="modal"` بدل `class="modal-overlay"`. قاعدة الإخفاء `display:none` موجودة على `.modal-overlay` فقط، فكان صندوق بيانات السائق يُرسم دائماً أسفل كل صفحة. صُحّح الصنف.

### مساحة سفلية

أُضيفت مساحة أسفل المحتوى تكفي لظهور آخر بطاقة كاملة:

- `.main` — 90px على سطح المكتب، 110px على الجوال
- تُضاف إليها `env(safe-area-inset-bottom)` لهواتف iPhone ذات الشريط السفلي
- الشريط الجانبي المنزلق حصل على مساحة سفلية أيضاً

## v4.3 — صور الأصناف من الإنترنت

### إصلاحان كانا يمنعان الميزة من العمل

1. **الخادم كان يتجاهل `image` عند إضافة صنف جديد.** كائن الصنف في `POST /api/items` لم يكن يتضمّن الحقل، فأي صنف تُنشئه يبدأ بلا صورة مهما كتبت في الحقل.
2. **حقل الصورة لا يُمسح عند فتح نافذة الإضافة.** تعدّل صنفاً له صورة ثم تضغط «إضافة صنف» فيبقى رابط الصنف السابق في الحقل.

### التنقية الأمنية

رابط الصورة يمرّ بـ `cleanImageUrl` على الخادم: يقبل `http://` و`https://` فقط، بحدّ 500 حرف. اختُبر برفض `javascript:alert(1)` و`data:text/html;base64,…` — كلاهما يُخزَّن فارغاً.

### طريقتان لإضافة صورة

**من البطاقة مباشرة (الأسرع):** اضغط على منطقة الصورة في أي بطاقة، تفتح نافذة صغيرة فيها معاينة كبيرة وحقل الرابط وزر «إزالة الصورة». على الشاشات اللمسية تظهر شارة «📷 أضف صورة» أو «🖼️ تغيير» دائماً؛ على سطح المكتب عند مرور الفأرة.

**من نافذة تعديل الصنف:** حقل الرابط بجانبه مربّع معاينة صغير.

### المعاينة الحية

في الحالتين تُحمَّل الصورة فعلياً قبل الحفظ وتظهر النتيجة:

- إطار أخضر + الصورة → الرابط يعمل
- إطار أحمر + «تعذّر تحميل الصورة» → الرابط مكسور أو الموقع يمنع الاستخدام الخارجي
- «رابط غير صالح» → لا يبدأ بـ http

المعاينة مؤجّلة 350ms بعد آخر ضغطة حتى لا تُحمَّل صورة مع كل حرف.

### الاحتياطي

الصنف بلا صورة يعرض رمز قسمه بلونه (🍰 وردي للحلويات، 🥙 برتقالي للشاورما…). والصورة التي تفشل في التحميل تسقط تلقائياً إلى الرمز نفسه عبر `onerror`، فلا تظهر بطاقة مكسورة للموظف أبداً.

**تلميح للمستخدم:** رابط صفحة نتائج جوجل لا يعمل — يجب رابط الصورة نفسه. من متصفح الحاسوب: زر الفأرة الأيمن على الصورة ← «نسخ عنوان الصورة». هذا مكتوب في النافذة نفسها.

## v4.4 — إصلاح صمت البوت وتحديث المنيو

### 1. البوت لا يرد — السبب والحل

`requireTrigger` و`browseOnly` كانا مفعّلين افتراضياً **بلا أي واجهة لإطفائهما**. أضفت الإعدادات ونسيت وسيلة التحكم بها، فبدا البوت معطّلاً وأنت لا تملك زراً لإعادته.

أُضيفت بطاقة **«🤖 سلوك البوت مع الزبائن»** في صفحة الإعدادات العامة، بأربعة توليفات مشروحة، ويظهر تحتها سطر يصف السلوك الحالي فوراً:

| يتطلب تفعيل | تصفّح فقط | السلوك |
|---|---|---|
| ✅ | ✅ | صامت حتى `bot`، ثم الأقسام فقط ← **الوضع الحالي** |
| ✅ | ❌ | صامت حتى `bot`، ثم كل القدرات (طلبات، أسعار، توصيل) |
| ❌ | ✅ | يرد على أي رسالة بقائمة الأقسام |
| ❌ | ❌ | **السلوك الكامل الأصلي** — يرد على كل شيء |

**إن كان الرقم مخصّصاً للمطعم، أطفئ «يتطلب كلمة تفعيل».** إن كان رقمك الشخصي، أبقِه مفعّلاً.

كلمة التفعيل صارت تعرض الأقسام في كل الأوضاع، فـ `bot` تعمل دائماً.

### 2. 🐛 حرف شارد كان يبتلع ردود الطلبات

في `handleMessage` سطر 2841 كان هناك `}f` — حرف `f` شارد بعد قوس الإغلاق. جافاسكربت تقبله نحوياً كتعبير، لكنه يرمي `ReferenceError: f is not defined` أثناء التنفيذ.

الأثر: أي زبون **داخل مرحلة الطلب** يرسل شيئاً غير مفهوم كان يفجّر استثناءً يبتلعه `try/catch` في مستقبل الرسائل، **فلا يصله رد إطلاقاً**. خطأ قديم لا علاقة له بالتعديلات، وظهر فقط عند اختبار المسار الكامل.

أُضيف أول سطر من `stack` إلى تسجيل الأخطاء ليسهل تتبّع مثله لاحقاً.

### 3. المنيو لا يتحدّث في مكان آخر

الواجهة كانت تخزّن الحالة في متغيّر `S` وتجلبها **مرة واحدة**: `if(!S) S = await apiGet('/state')`. فإذا أضفت صنفاً من جهاز، والجهاز الآخر عليه اللوحة مفتوحة، يبقى يعرض النسخة القديمة حتى إعادة التحميل اليدوية.

تحققت من طبقة البيانات أولاً: الأصناف الجديدة تُحفظ في Firebase، وتنجو من إعادة التشغيل، والبوت يعرضها — المشكلة كانت في الواجهة وحدها.

الإصلاح:

- `loadMenu()` تجلب أحدث نسخة في كل مرة تُفتح فيها الصفحة.
- زر **🔄 تحديث** وختم وقت آخر تحديث في ترويسة المنيو.
- استطلاع كل 30 ثانية أثناء فتح الصفحة، يقارن بصمة المنيو ولا يعيد الرسم إلا عند تغيّر فعلي، ويُظهر «🔄 المنيو تحدّث من جهاز آخر». الحساب لا تُقاطَع فلترته أو تمريره بلا سبب.
- صفحة الأقسام تجلب بيانات جديدة أيضاً.
- بعد حفظ أي صنف تُعاد القراءة من الخادم بدل الاكتفاء بالنسخة المحلية.

## v4.5 — 🚨 إصلاح فقدان البيانات عند انقطاع Firebase

### السبب

سطر واحد في `loadState`:

```js
} catch(e) { console.log('⚠️ Firebase loadState:', e.message); }
```

كان يبتلع فشل القراءة ويكمل التشغيل. التسلسل الكارثي:

1. Firebase تفشل قراءتها لحظياً (بداية باردة بعد نوم Render، انقطاع شبكة، تجاوز حصة).
2. الخطأ يُبتلع، فتبقى `STATE` على **البيانات الافتراضية المكتوبة في الكود**.
3. `auth.init` يرى `STATE.users` فارغة فينشئ الحسابات الأربعة بكلمات المرور الافتراضية.
4. أول `saveState()` — بعد 3 ثوانٍ — يكتب `STATE_DOC.set(STATE)` **فوق قاعدتك كاملة**.

النتيجة: المنيو يعود لنسخة الكود، وكلمات المرور تعود للافتراضية، وشواهد الحذف تُفقد. وكان السجل يطبع `✅ state محمّل من Firebase` رغم الفشل، فلا شيء ينبّهك.

أُعيد إنتاج الخلل باختبار يُفشل قراءة واحدة: منيو مخصّص من صنفين وكلمة مرور مُغيّرة → بعد التشغيل 69 صنفاً افتراضياً وكلمة مرور افتراضية.

### الإصلاح

**قفل الكتابة.** لا كتابة على Firebase قبل قراءة ناجحة مؤكَّدة. `saveState` و`saveStateNow` ترفضان وتسجّلان `⛔ حفظ مرفوض`.

**إعادة محاولة متدرّجة.** خمس محاولات بفواصل 2 ← 4 ← 8 ← 15 ثانية. تكفي لتجاوز البداية الباردة.

**فشل آمن.** إن فشلت كلها: الخدمة تبقى تعمل للاطلاع، الكتابة مقفلة، **البوت لا يبدأ** (حتى لا يخدم الزبائن بمنيو خاطئ)، والمحاولة تستمر كل 30 ثانية. عند النجاح يُفتح القفل ويبدأ البوت تلقائياً.

**تنبيه ظاهر.** شريط أحمر أعلى اللوحة يوضّح المشكلة وسببها، مربوط بحقلَي `dbReady` و`dbError` في `/api/status`.

**حفظ عند الإغلاق.** Render يرسل `SIGTERM` قبل إيقاف الخدمة. أي حفظ مؤجّل (٣ ثوانٍ) كان يضيع؛ الآن يُكتب فوراً عبر `flushState()`.

### التحقق

| الحالة | قبل | بعد |
|---|---|---|
| فشل قراءة واحد | ❌ محو كامل | ✅ نجحت المحاولة الثانية، البيانات سليمة |
| فشل كل المحاولات | ❌ محو كامل | ✅ القاعدة لم تتغيّر، البوت متوقف، المحاولة مستمرة |
| إيقاف الخدمة أثناء حفظ مؤجّل | ❌ يضيع | ✅ يُكتب قبل الإغلاق |

### ماذا تفعل الآن

بياناتك الحالية هي النسخة الافتراضية التي كُتبت فوق الأصل — لا يمكن استرجاعها من الكود. أعد إدخال المنيو وكلمات المرور مرة واحدة، ولن يتكرر المحو.

للحماية المستقبلية: Firebase Console ← Firestore ← Backups، فعّل نسخة احتياطية يومية (متاحة في الخطة المجانية بحدود).

## v5.0 — المنيو الرسمي بفرعين

استُبدل المنيو التجريبي (69 صنفاً) ببيانات المطعم الحقيقية المأخوذة من ملف الموقع.

### الأرقام

| | فرع غزة | الفرع الأوسط |
|---|---|---|
| الشاورما | 12 | 12 |
| الإيطالي | 9 | 10 |
| السندويشات الغربية | 7 | 7 |
| السلطات | 8 | 8 |
| حلويات البار | 16 | 16 |
| الكيك والحلويات الغربية | 13 | 13 |
| الحلويات الشرقية | 23 | 23 |
| الجيلاتو | 7 | 7 |
| المشروبات | 21 | 26 |
| **الإجمالي** | **116** | **122** |

238 صنفاً، 46 بسعر الكيلو، 14 بأحجام متعددة، 57 بوصف، وكلها بمسار صورة. حالات `active: false` منقولة كما هي (19 صنفاً مغلقاً).

### ثلاثة أنواع أسعار

```
عادي   → باشكا ............ 40 ₪
بالوزن → نمورة ............ 20 ₪/كغم
بأحجام → سلطات مشكلة ...... كبير 15 · وسط 10 ₪
```

قسم الحلويات الشرقية يُكتب بجانبه «الأسعار بالكيلو». والوصف يظهر تحت الاسم بخط مائل، ويمكن إطفاؤه من الإعدادات.

### الفروع

الفرعان مستقلان تماماً في قاعدة البيانات: إغلاق «باشكا» في غزة لا يؤثر على الأوسط. هذا مقصود — نفاد صنف يحدث في فرع لا في الاثنين.

**في المحادثة:** البوت يسأل الزبون عن فرعه أول المحادثة، ثم يعرض منيو ذلك الفرع فقط. `99` تغيّر الفرع في أي وقت (الرقم 9 محجوز للمشروبات).

لو كان الرقم يخدم فرعاً واحداً: أطفئ «تعدّد الفروع» من الإعدادات واختر الفرع، فيختفي السؤال.

**في اللوحة:** تبويب فوق الأقسام — كل الفروع / فرع غزة / الفرع الأوسط، مع عدد أصناف كل فرع ونقطة حمراء إن كان فيه أصناف مغلقة. وعند عرض «كل الفروع» يظهر اسم الفرع تحت كل بطاقة.

### الصور

مسارات الصور نسبية (`/menu/shawarma/48.jpg`) لأنها من موقعكم. أُضيف حقل **«نطاق صور المنيو»** في الإعدادات — ضع فيه عنوان موقعكم فتظهر الصور الـ238 كلها دفعة واحدة. اتركه فارغاً فيظهر رمز القسم بلونه.

الروابط المطلقة (`https://…`) تعمل كما هي دون النطاق.

### الترحيل

طريقان:

- **قاعدة جديدة** — المنيو الجديد هو الأصل المكتوب في الكود.
- **قاعدتك الحالية** — يُكتشف اختلاف `menuVersion` فيُستبدل المنيو مرة واحدة، وتُحفظ النسخة القديمة في `itemsBackup` داخل نفس المستند قبل الاستبدال.

الترحيل لا يمسّ الحسابات ولا الطلبات ولا الردود ولا السائقين ولا سجل التغييرات.

### ملاحظتان على البيانات الأصلية

- كان هناك صنفان باسم «صحن شاورما» (30 ₪ نشط، و20 ₪ مغلق). سُمّي الثاني **«صحن شاورما صغير»** ليتمكن البوت من التمييز بينهما.
- الأسماء ذات المسافات الزائدة (` صحن شاورما`، `ذرة مايونيز `) نُظّفت.

### ملفان جديدان

```
menu-source.js   بيانات المنيو كما هي من الموقع — عدّلها هنا لأي تحديث شامل
menu-build.js    يحوّلها لصيغة البوت ويولّد كلمات البحث. شغّله وحده لتقرير تحقّق:
                 node menu-build.js
```

## v5.1 — رسالة الدخول المضلّلة وطريق الاسترجاع

### السبب

عند تعذّر تحميل البيانات من Firebase، كانت `STATE.users` فارغة تماماً — لأن `auth.init` لا يعمل إلا بعد قراءة ناجحة. أي محاولة دخول كانت تمرّ على قائمة حسابات فارغة فترجع:

> اسم المستخدم أو كلمة المرور غير صحيحة

وهي رسالة خاطئة تماماً: الحساب سليم في قاعدتك، والنظام لم يقرأه بعد. هذا يحدث كثيراً على Render المجاني — الطلب الأول يوقظ الحاوية، وقراءة Firestore تأخذ ثوانٍ، فمن يفتح اللوحة في تلك اللحظة يظن أن حسابه ضاع.

### الإصلاح

**رسالة صادقة.** الدخول قبل جاهزية القاعدة يرجع 503 مع: «النظام لم يُحمّل بياناته بعد من Firebase — انتظر ثوانٍ وأعد المحاولة»، مع السبب التقني إن وُجد.

**صفحة الدخول تعرف الحالة.** نقطة `/api/dbstatus` متاحة بلا تسجيل دخول، وصفحة الدخول تستطلعها كل 4 ثوانٍ. أثناء التحميل يتحوّل الزر إلى «بانتظار البيانات…» ويظهر شريط أصفر يشرح، ثم يعود الزر للعمل تلقائياً عند الجاهزية.

**قائمة الحسابات في السجل.** عند كل إقلاع تُطبع الحسابات الموجودة فعلاً:

```
👥 الحسابات: murad(سوبر أدمن)، cashier(كاشير، كلمة مرور افتراضية)، …
```

فتعرف من سجل Render بماذا تسجّل الدخول.

### 🔑 استرجاع الطوارئ

إن فقدت كلمة المرور فعلاً (مثلاً بعد المحو الذي كان يحدث قبل v4.5):

1. Render ← **Environment** ← أضف `RESET_ADMIN_PASSWORD` بقيمة كلمة المرور الجديدة (6 أحرف فأكثر)
2. احفظ — تُعاد الخدمة تلقائياً
3. سجّل الدخول بـ `murad` وكلمة المرور الجديدة
4. **احذف المتغيّر من Render** — وإلا أُعيدت كلمة المرور مع كل نشر

`RESET_ADMIN_USER` اختياري لاستهداف حساب آخر (الافتراضي `murad`؛ وإن لم يوجد يُختار أول سوبر أدمن).

## v5.2 — تشخيص أخطاء Firebase

### الخطأ الذي ظهر

```
16 UNAUTHENTICATED: Request had invalid authentication credentials.
```

معناه واحد: **مفتاح الخدمة في `FIREBASE_SERVICE_ACCOUNT` لم يعد صالحاً**. أشيع سبب: إبطال المفتاح القديم من Google Cloud دون وضع الجديد في Render.

### فحص المفتاح عند الإقلاع

قبل الاتصال، يُفحص المتغيّر ويُشرح أي خلل بدقة بدل الانهيار الغامض:

- غير موجود → أين تضيفه وماذا تضع فيه
- ليس JSON صالحاً → موضع الخطأ مع تنبيه ألّا تضع اقتباسات حول المحتوى
- ناقص `project_id` أو `client_email` أو `private_key`
- `private_key` لا يبدأ بـ `-----BEGIN PRIVATE KEY-----`
- أسطر `private_key` مكسورة إلى `\n` نصية → **تُصحَّح تلقائياً** (خطأ شائع عند اللصق في لوحات الاستضافة)

وعند نجاح الفحص يُطبع الحساب والمشروع لتتأكد أنهما الصحيحان:

```
🔑 مفتاح Firebase: firebase-adminsdk-fbsvc@o2bot-b7a51.iam.gserviceaccount.com
   المشروع: o2bot-b7a51
```

### ترجمة أخطاء الاتصال

بدل عرض رسالة gRPC الخام، يُصنَّف الخطأ وتُعرض خطواته في السجل **وفي صفحة الدخول**:

| الخطأ | التشخيص | يُصلحه الانتظار؟ |
|---|---|---|
| `UNAUTHENTICATED` | مفتاح غير صالح أو مُبطَل | ❌ يحتاج تدخلك |
| `PERMISSION_DENIED` | المفتاح صالح بلا صلاحية Firestore | ❌ |
| `NOT_FOUND` | قاعدة Firestore غير منشأة | ❌ |
| غير ذلك | انقطاع مؤقت | ✅ يعيد المحاولة |

الأخطاء التي لا يُصلحها الانتظار تظهر بشريط أحمر مع خطوات مرقّمة، بدل الشريط الأصفر الذي يوحي بأن الانتظار يكفي.

## v5.3 — فاحص مفتاح Firebase

ملف جديد `test-firebase.js` يحسم إن كان المفتاح ما زال يعمل، قراءةً فقط بلا أي كتابة:

```bash
node test-firebase.js key.json          # من ملف
node test-firebase.js                   # من FIREBASE_SERVICE_ACCOUNT
```

عند النجاح يطبع المشروع وحساب الخدمة وعدد الأصناف والحسابات والطلبات وحجم المستند. وعند الفشل يصنّف السبب: مفتاح مُبطَل، أو صلاحية ناقصة، أو قاعدة غير منشأة، أو حجب شبكة على جهازك — كلٌّ بحلّه.

مفيد قبل النشر: افحص المفتاح محلياً بدل اكتشاف الخلل من سجل Render.

## v5.4 — منع تسريب المفاتيح إلى GitHub

GitHub رفض الرفع لأن ملف `FIREBASE_SERVICE_ACCOUNT-سطر-واحد.txt` دخل الكوميت. **الرفض حماك: المفتاح لم يصل GitHub، هو في تاريخك المحلي فقط.**

### ⛔ لا تضغط رابط unblock-secret

الرابط الذي عرضه GitHub (`security/secret-scanning/unblock-secret/…`) يسمح بالرفع فعلاً. الضغط عليه ينشر المفتاح.

### التنظيف

الكوميتات المحلية لم تُرفع، فالحل بسيط ولا يحتاج أدوات خاصة:

```bash
# 1) استثنِ المفاتيح مستقبلاً (الحزمة الجديدة فيها هذا جاهزاً)
cp .gitignore-الجديد .gitignore

# 2) ارجع لآخر حالة مرفوعة — ملفاتك تبقى كما هي، الكوميتات فقط تُلغى
git reset --soft origin/main

# 3) أخرج ملف المفتاح من المرحّلة وانقله خارج المشروع
git rm --cached "FIREBASE_SERVICE_ACCOUNT-سطر-واحد.txt"
mkdir -p ~/keys && mv "FIREBASE_SERVICE_ACCOUNT-سطر-واحد.txt" ~/keys/

# 4) كوميت نظيف وارفع
git add -A
git commit -m "تحديث البوت"
git push
```

`reset --soft` يحتفظ بكل تعديلاتك كملفات مرحّلة ويلغي الكوميتات المحلية فقط — لا تفقد شيئاً من عملك.

### فاحص قبل الرفع

`check-secrets.sh` يفحص المجلد وقائمة git وتاريخ الكوميتات:

```bash
bash check-secrets.sh
```

يخرج بحالة فشل إن وجد مفتاحاً متتبَّعاً أو أثراً في التاريخ، فيصلح لوضعه في `pre-push` hook:

```bash
cp check-secrets.sh .git/hooks/pre-push && chmod +x .git/hooks/pre-push
```

### `.gitignore` موسّع

```
FIREBASE_SERVICE_ACCOUNT*
*firebase-adminsdk*
*service-account*
*-adminsdk-*
*.key.json
*.pem
secrets/
keys/
```

### أين يُحفظ المفتاح

خارج مجلد المشروع تماماً — `~/keys/` مثلاً. مكانه الوحيد في التشغيل هو متغيّر البيئة `FIREBASE_SERVICE_ACCOUNT` على Render.

## v5.5 — لماذا لم تظهر الصور

سببان معاً:

### 1. 🐛 مسارات الصور تشوّهت

99 مساراً من أصل 109 في `menu-source.js` صار شكله `public/menu/italian/91.jpeg` بدل `/menu/italian/91.jpeg`.

`public` هو **جذر النشر** لا جزء من العنوان. الرابط الناتج كان:

```
https://o2bot-b7a51.web.app/public/menu/italian/91.jpeg   ← 404
                            ^^^^^^^ زائد
```

صُحّح 160 موضعاً. المسارات الآن كلها تبدأ بـ `/menu/`.

### 2. الصور لم تُرفع أصلاً

لا مجلد `.firebase` في المشروع، أي أن `firebase deploy` لم يُنفَّذ. و`.firebaserc` كان مفقوداً — وهو ملف مخفي ضاع عند إعادة الضغط — فكان `deploy` سيفشل بـ `No project active` حتى لو جُرّب.

كلاهما مُصلح في هذه الحزمة.

### فاحص جديد

```bash
node check-images.js                              # فحص محلي
node check-images.js https://o2bot-b7a51.web.app  # + فحص النطاق فعلياً
```

يفحص أربع مراحل ويحدّد أين انقطعت السلسلة:

1. الصور على القرص وعددها وحجمها
2. **شكل المسارات** — يكشف `public/menu/…` وأمثاله ويطبع الصحيح بجانب الخاطئ
3. جاهزية النشر — `firebase.json` و`.firebaserc` و`.firebase`
4. النطاق فعلياً — يطلب ثلاث صور ويتحقق أن الرد 200 ونوعه `image/*` لا HTML

### الترتيب الصحيح للنشر

```bash
firebase login
firebase use --add                  # اختر o2bot-b7a51
firebase deploy --only hosting
node check-images.js https://o2bot-b7a51.web.app
```

ثم: اللوحة ← الإعدادات ← **نطاق صور المنيو** ← الصق الرابط ← حفظ.

## v5.6 — إصلاح المسارات المحفوظة + صفحة تحقّق

### المشكلة المتبقية بعد النشر

تصحيح `menu-source.js` لا يكفي: المنيو **مُرحَّل بالفعل إلى Firestore** بالمسارات المشوّهة، و`menuVersion` مضبوط فلن يُعاد الترحيل — وهذا مقصود حتى لا تضيع حالات الإغلاق التي ضبطها الطاقم.

الحل: **إصلاح موضعي عند كل تحميل**. أي مسار لا يبدأ بـ `/menu/` ولا بـ `http` يُعاد تشكيله:

```
public/menu/italian/91.jpeg  →  /menu/italian/91.jpeg
menu/sweets/9.jpg            →  /menu/sweets/9.jpg
```

آمن ويتكرر بلا ضرر، ولا يمسّ الروابط المطلقة ولا الأصناف بلا صورة ولا حالات التفعيل. اختُبر: صنف مغلق بقي مغلقاً باسم من أغلقه.

يظهر في السجل عند حدوثه:

```
🖼️  صُحّح 99 مسار صورة (public/menu/… ← /menu/…)
```

### صفحة تحقّق بصرية

أُضيف `public/index.html`. بعد النشر افتح `https://o2bot-b7a51.web.app` مباشرة فترى الصور الـ109 كلها في شبكة واحدة — أي صورة مكسورة تظهر فوراً. الصفحة تطبع أيضاً النطاق الذي تنسخه للإعدادات.

كان النطاق الأساسي يرجع 404 لعدم وجود `index.html`، فيظن المرء أن النشر فشل.

### Render و Firebase معاً

لا تعارض: **Render** يشغّل البوت واللوحة، و**Firebase Hosting** يقدّم الصور الثابتة فقط. اللوحة تُحمَّل من Render وتطلب الصور من `web.app` — وسم `<img>` لا يحتاج CORS، والترويسة `Access-Control-Allow-Origin: *` مضبوطة على أي حال.

Firebase Hosting هنا بديل عن رفع 8 ميغابايت من الصور إلى Render، حيث القرص مؤقت وتُمحى مع كل نشر.

## v5.7 — لماذا لا تظهر الصور رغم نجاح النشر

الصور على `https://o2bot-b7a51.web.app` بالفعل. المشكلة في الطرف الآخر: **Render ما زال يشغّل الكود القديم.**

آخر ما رُفع إلى GitHub هو `4fa301b`، وهو يسبق إصلاح المسارات. Render ينشر من GitHub، فالكود الذي يخدم اللوحة لا يعرف الإصلاح، والمنيو في Firestore ما زال يحمل `public/menu/…`، فالرابط الناتج:

```
https://o2bot-b7a51.web.app/public/menu/italian/91.jpeg   → 404
```

### إصلاح مزدوج

**في الخادم** (`index.js`) — يصحّح المسارات في قاعدة البيانات عند كل تحميل.

**في الواجهة** (`dashboard.html`) — دالة `normImagePath` تصحّح المسار وقت العرض حتى لو بقي مشوّهاً في القاعدة. اختُبرت على `public/menu/…` و`menu/…` و`/public/menu/…`، وتترك الروابط المطلقة والحقول الفارغة كما هي.

### زر «🔍 اختبر»

بجانب حقل نطاق الصور في الإعدادات. يأخذ أول صنف له صورة، يبني الرابط الحقيقي، **يحمّله فعلاً**، ويعرض:

- المسار كما هو محفوظ في قاعدة البيانات
- تنبيهاً أصفر إن كان مشوّهاً مع الشكل المصحَّح
- الرابط النهائي كاملاً
- ✅ مع معاينة الصورة، أو ❌ مع احتمالات السبب مرتّبة

يحوّل «الصور مش ظاهرة» إلى إجابة محدّدة بضغطة واحدة.

### الترتيب

1. انسخ ملفات v5.7 فوق مشروعك
2. `git add -A && git commit -m "إصلاح مسارات الصور" && git push`
3. انتظر Render حتى ينتهي النشر (راقب Logs حتى `✅ السيرفر شغال`)
4. اللوحة ← الإعدادات ← نطاق صور المنيو ← `https://o2bot-b7a51.web.app` ← حفظ
5. اضغط **🔍 اختبر**
6. صفحة المنيو ← **🔄 تحديث**

في سجل Render عند أول تشغيل سترى: `🖼️ صُحّح 99 مسار صورة`

## v5.8 — رفع الصور من الجهاز + إصلاح الدراور

### 🐛 الدراور لا يستجيب على الجوال

```css
.layout{ … position:relative; z-index:1 }
```

هذا السطر يصنع **سياق تكديس** (stacking context). الشريط الجانبي داخله بـ`z-index:99`، لكن الرقم محبوس داخل السياق ولا يتجاوز `z-index:1` عند الجذر. والخلفية المعتمة `#sb-backdrop` شقيقة لـ`.layout` بـ`z-index:98`.

النتيجة عند الجذر: `98 > 1` — **الخلفية المعتمة تُرسم فوق الدراور بالكامل**، فكل ضغطة تسقط عليها لا على أزرار التنقّل.

الإصلاح: أُزيل `z-index` من `.layout` فلم يعد يصنع سياقاً، ووُحّدت الطبقات:

| العنصر | z-index |
|---|---|
| الخلفية المعتمة | 90 |
| الدراور | 95 |
| الترويسة | 100 |

### رفع صورة من الجهاز

نافذة الصورة صار فيها تبويبان: **📁 من الجهاز** و**🔗 رابط**.

الملف يُصغَّر **في المتصفح** إلى 800px بجودة 80 قبل الرفع (صورة 3 ميغابايت تصير ~90 ك.ب)، ثم يُرسل ويُحفظ في Firestore.

**لماذا Firestore لا Storage:** Storage صار يتطلب Blaze. Firestore مجاني على Spark بـ1 غيغابايت. كل صورة في مستند مستقل داخل مجموعة `o2bot_images`، فلا تتضخّم وثيقة `state` ولا تصطدم بحد الميغابايت.

المسار المحفوظ يصير `/api/img/<id>` ويُخدَم من الخادم نفسه — لا يُسبق بنطاق Firebase، ويحمل `Cache-Control: immutable` فيُحمَّل مرة واحدة.

**الحماية:**

| الفحص | النتيجة |
|---|---|
| `data:text/html;base64,…` | مرفوض |
| نص ليس data URL | مرفوض |
| أكبر من 600 ك.ب | مرفوض برسالة تذكر الحجم |
| جسم طلب أكبر من 1.5 م.ب | 413 ويُقطع الاتصال |
| حساب كاشير | 403 — الرفع يحتاج `menu.edit` |

اختُبر الاسترجاع بمقارنة البايتات: الصورة المسترجعة مطابقة للمرفوعة تماماً.

### الطرق الثلاث للصورة

| الطريقة | أين تُخزَّن | متى تستخدمها |
|---|---|---|
| Firebase Hosting | `/menu/…` على النطاق | الـ109 صورة الأصلية |
| رفع من الجهاز | Firestore، `/api/img/<id>` | صنف جديد أو تغيير صورة بسرعة |
| رابط خارجي | لا شيء | صورة على موقع آخر |

الثلاث تعمل معاً في المنيو نفسه.

## v5.9 — إصلاح تكرار «انقطع الاتصال (408)»

408 يعني انتهاء مهلة الاتصال. الانقطاع نفسه طبيعي أحياناً، لكن الكود كان يحوّله إلى حلقة لا تنتهي.

### السبب الأول: لا حارس ضد الاتصالات المتوازية

ستة مواضع تستدعي `startBaileys` بلا أي تنسيق، ومعالج القطع كان:

```js
setTimeout(startBaileys, 5000);   // بلا حارس، وبلا إغلاق السوكيت القديم
```

كل انقطاع ينشئ سوكيتاً جديداً **دون إغلاق القديم**. السوكيت القديم يبقى يبثّ أحداثاً، فتتراكم اتصالات تتنافس على نفس جلسة واتساب — وواتساب يقطعها جميعاً. النتيجة: 408 يولّد 408.

**الإصلاح:**

- `waConnecting` يرفض أي محاولة أثناء محاولة جارية
- `killSocket()` يزيل المستمعين ويغلق السوكيت قبل فتح جديد
- تراجع تدريجي: 5 ← 10 ← 20 ← 40 ← 60 ثانية بدل 5 ثوانٍ ثابتة
- العدّاد يُصفَّر عند نجاح الاتصال

اختُبر بعاصفة خمسة انقطاعات متتالية: **سوكيت واحد** بدل ستة.

### السبب الثاني: النداء الذاتي معطّل

```js
http.get(SELF_URL + '/ping', …).on('error', () => {});
```

على Render يكون `SELF_URL` بـ`https`، و`http.get` يفشل عليه — والخطأ مُبتلَع بمعالج فارغ. أي أن الخدمة كانت **تنام رغم وجود النداء**، وواتساب ينقطع بـ408 عند الإيقاظ.

صار يختار الوحدة حسب البروتوكول، والفترة 12 دقيقة بدل 14 (هامش أمان تحت حد الـ15)، ويسجّل الفشل بدل ابتلاعه.

### السبب الثالث: خيارات السوكيت

```js
defaultQueryTimeoutMs: undefined,   // المهلة القصيرة أشيع سبب لـ 408
markOnlineOnConnect:   false,       // لا تسحب كل الإشعارات دفعة واحدة
syncFullHistory:       false,       // لا نحتاج تاريخ المحادثات
emitOwnEvents:         false,
```

### أسباب الانقطاع صارت مفهومة

| الرمز | المعنى | التصرّف |
|---|---|---|
| 408 | انتهت مهلة الاتصال | إعادة محاولة بتراجع |
| 428 | أُغلق الاتصال | إعادة محاولة |
| 440 | **جلسة أخرى بنفس الرقم** | تحذير + محاولة واحدة |
| 401 | تسجيل خروج من الهاتف | **يتوقف** ويطلب إعادة الربط |
| 515 | يحتاج إعادة تشغيل | إعادة محاولة |

اختُبر 401: صفر محاولات إعادة — كان قبلها يعيد المحاولة إلى الأبد بلا فائدة.

### إن استمر بعد التحديث

الرمز **440** يعني أن نفس رقم واتساب مرتبط بخدمة أخرى — نسخة قديمة على Render، أو تشغيل محلي، أو WhatsApp Web مفتوح في متصفح. الاثنان يطردان بعضهما بلا نهاية. أوقف واحدة.

`/api/status` صار يُرجع `waStats` بعدد الانقطاعات وآخر رمز ووقته، لتعرف النمط بدل التخمين.

## v6.0 — بطاقة صحة الاتصال

### الجواب: نعم، هاتفك لا يحتاج أن يبقى متصلاً

واتساب متعدّد الأجهزة يجعل الجهاز المرتبط يعمل والهاتف مغلق أو بلا إنترنت. الرسائل تصل لكل جهاز مرتبط على حدة، ولو كان أحدها غير متصل استلم كل المعلّق فور عودته.

**القيد الوحيد:** إذا بقي الهاتف بلا إنترنت **14 يوماً**، يفصل واتساب كل الأجهزة المرتبطة أمنياً (خطأ 401). يكفي فتح واتساب على الهاتف دقيقة واحدة ليُصفَّر العدّاد.

فانقطاعات 408 لم تكن من الهاتف قط. كانت من نوم Render ومن عاصفة إعادة الاتصال — وكلاهما مُصلح في v5.9.

### بطاقة «📶 صحة اتصال واتساب»

في أعلى لوحة التحكم، تعرض:

- **الحالة الحالية** ومدة الاتصال المتواصل
- **انقطاعات آخر ٢٤ ساعة** · الإجمالي · كم مرة عاد تلقائياً
- **سجل آخر 5 انقطاعات** برمزها وسببها ووقتها
- **تنبيه قاعدة الـ14 يوماً** يظهر تلقائياً بعد 11 يوماً من الربط

الصفحة تتحدّث كل 5 ثوانٍ مع الاستطلاع القائم.

### ما تخبرك به الأرقام

| ما تراه | المعنى |
|---|---|
| انقطاعات قليلة و«عاد تلقائياً» بنفس العدد | طبيعي — الشبكة تتذبذب والنظام يتعافى |
| انقطاعات كثيرة برمز **408** | الخدمة تنام. تأكد أن `RENDER_EXTERNAL_URL` مضبوط |
| رمز **440** متكرر | نفس الرقم مرتبط بجلسة أخرى — أوقف إحداهما |
| رمز **401** | مضى 14 يوماً على الهاتف — افتح واتساب عليه ثم أعد الربط من `/link` |
| «غير متصل» ولا محاولات | البيانات لم تُحمّل من Firebase — البوت لا يبدأ عمداً |

## v6.1 — استقلال البوت عن الهاتف

### الجواب المختصر

**البوت لا يحتاج هاتفك متصلاً.** واتساب متعدّد الأجهزة يجعل الجهاز المرتبط مستقلاً — الشرط الوحيد أن يتصل الهاتف بالإنترنت مرة كل 14 يوماً وإلا فُصلت كل الأجهزة (خطأ 401 لا 408).

### فحص الكود

بحثت في `index.js` عن كل ما قد يعتمد على الهاتف: `presenceSubscribe` · `onWhatsApp` · `fetchStatus` · `profilePicture` · `groupMetadata` · `chatModify` · `readMessages`.

**لا شيء منها مستخدم.** مسار الرسالة كله عمليات خادم: استقبال ← معالجة ← `sendMessage`.

### 🐛 لكن وجدت ما يُسقط الردود

```js
await waSocket.sendPresenceUpdate('composing', jid);   // ← يُنتظَر
…
const reply = await handleMessage(msgObj);
```

مؤشر الكتابة **تجميلي**، لكنه كان يُنتظَر قبل الرد وداخل نفس `try`. فإن تأخّر أو فشل — وهو أول ما يفشل على شبكة متعثّرة — يقفز التنفيذ إلى `catch` و**لا يصل الزبون أي رد**.

هذا يفسّر شعورك بأن البوت «يتوقف» عند اضطراب الشبكة، دون أن ينقطع الاتصال فعلياً.

**الإصلاح:**

- `presence()` تُطلق ولا تُنتظَر، وفشلها لا يعني شيئاً
- `sendText()` تعيد المحاولة مرة بعد ثانيتين، وتسجّل بوضوح إن ضاع رد

اختُبر بجعل مؤشر الكتابة يفشل في كل مرة: **الردود تصل كاملة**. قبل الإصلاح كانت تضيع جميعها.

### كيف تتحقق بنفسك

ضع هاتف المطعم في **وضع الطيران**، ثم أرسل `bot` من هاتف آخر. يجب أن يرد خلال ثوانٍ. إن رد فالاستقلال مؤكَّد.

## v6.2 — «لماذا لم يرد البوت؟»

كانت هناك **ست نقاط تُسقط الرسالة بصمت**، بلا سجل ولا تفسير:

| النقطة | كانت |
|---|---|
| `msg.key.fromMe` | `continue` صامت |
| رسالة مجموعة | `continue` صامت |
| رسالة بلا نص | `continue` صامت |
| بلا معرّف مرسل | `continue` صامت |
| `botActive = false` | `return null` |
| ليست كلمة تفعيل | `return null` |

النتيجة: ترسل «منيو» فلا يرد شيء، ولا سبيل لمعرفة السبب.

### السجل الجديد

كل إسقاط يُسجَّل الآن بالوقت والرقم (مقنَّعاً) والنص والسبب **وكيفية الإصلاح**. تظهر بطاقة **🔇 لماذا لم يرد البوت؟** في لوحة التحكم تلقائياً عند وجود رسائل مُهملة.

```
«منيو»           → رسالة صادرة من الرقم المربوط نفسه
                   البوت لا يرد على رسائلك أنت. اختبره من هاتف آخر.
«بوت»            → رسالة من مجموعة
                   البوت يتجاهل المجموعات عمداً.
«مرحبا كيف الحال» → ليست كلمة تفعيل
                   الكلمات المقبولة: bot، بوت، o2، منيو، menu
```

وتُظهر أعلى البطاقة تنبيهاً إن كان البوت موقوفاً، أو تسرد كلمات التفعيل المضبوطة فعلاً في قاعدة بياناتك — لا الافتراضية.

### الأسباب الثلاثة الأشيع لصمت البوت

**١. أرسلتَ من الرقم المربوط نفسه.** أشيعها. `fromMe` يجعل البوت يتجاهل رسالتك. اختبره دائماً من هاتف آخر.

**٢. البوت موقوف.** زر «إيقاف البوت» في أعلى اللوحة يبقى موقوفاً بعد إعادة التشغيل لأن الحالة محفوظة.

**٣. كلمات التفعيل مختلفة عمّا تظنّ.** البطاقة تعرض المضبوط فعلاً في قاعدة بياناتك.

نقطة جديدة: `GET /api/dropped` تُرجع آخر 25 رسالة مُهملة مع الإعدادات الفعّالة.

## v6.3 — تسريع الربط وإظهار مراحله

### أين كان يذهب الوقت

**١. جلب نسخة واتساب في كل اتصال.** `fetchLatestBaileysVersion()` يطلب النسخة من الإنترنت عند كل محاولة، **بلا مهلة**. على شبكة بطيئة يضيف ثوانيَ قبل ظهور QR، وقد يعلّق تماماً.

صار مخبّأً 6 ساعات، بمهلة 8 ثوانٍ، وبنسخة احتياطية عند الفشل. اختُبر بمحاكاة جلب يستغرق 6 ثوانٍ: الاتصال الأول يدفع الثمن مرة واحدة، وكل إعادة اتصال بعده **توفّر 6 ثوانٍ**.

**٢. كتابة الترحيل تحجب بدء البوت.**

```js
if (migrationPending) { await saveStateNow(); }   // ← يُنتظَر
startBaileys();
```

كتابة 238 صنفاً + النسخة الاحتياطية إلى Firestore كانت تؤخّر بدء الاتصال. صارت تجري في الخلفية.

**٣. نوم Render.** الخطة المجانية توقف الخدمة بعد 15 دقيقة، فأول طلب يوقظها في 30-60 ثانية قبل أن يبدأ أي شيء. النداء الذاتي المُصلح في v5.9 يمنع هذا — تأكد أن `RENDER_EXTERNAL_URL` مضبوط.

### شريط المراحل في `/link`

بدل «جاري القراءة…» الغامضة، ترى أين وصلت بالضبط:

```
✓ تحميل البيانات من Firebase
✓ قراءة جلسة واتساب
✓ جلب نسخة واتساب ويب
◉ الاتصال بخوادم واتساب        7ث
  الرمز أو الكود جاهز
  مرتبط
```

المرحلة الحالية تنبض ويظهر بجانبها عدّاد بالثواني. وإن تجاوزت 45 ثانية يظهر تنبيه يشرح أن Render المجاني يحتاج حتى دقيقة للاستيقاظ، ومتى يستحق الأمر مراجعة السجل.

سجل Render صار يطبع زمن كل مرحلة أيضاً:

```
✅ state محمّل من Firebase (2ms)
   ⏱️  جلب نسخة واتساب ويب
   نسخة واتساب 2.3000.1043857760 (78ms)
   ⏱️  فتح الاتصال بخوادم واتساب
```

## v6.4 — `doctor.js` فحص شامل بأمر واحد

انكسار الربط والمنيو والصور **معاً** يشير غالباً لسبب واحد لا ثلاثة. السبب الأشيع: تعذّر تحميل البيانات من Firebase — عندها يرفض النظام تشغيل البوت عمداً (حماية من محو البيانات)، فيبدو كل شيء معطّلاً.

```bash
node doctor.js https://o2bot.onrender.com murad كلمة_المرور
```

قراءة فقط، ولا يغيّر شيئاً. يفحص ستة مستويات بالترتيب:

| # | الفحص |
|---|---|
| 1 | هل الخدمة حيّة؟ وكم استغرقت (يكشف النوم) |
| 2 | Firebase — متصل؟ وإن لا، لماذا وكيف يُصلح |
| 3 | الدخول بحسابك |
| 4 | واتساب — مرتبط؟ في أي مرحلة؟ وتحليل الانقطاعات برموزها |
| 5 | المنيو والصور — الأقسام، العناوين، المسارات المشوّهة، **ويحمّل صورة نموذجية فعلياً** |
| 6 | الرسائل المُهملة والإعدادات الفعّالة |

مثال على مخرجاته عند فشل Firebase:

```
════ 2) قاعدة البيانات ════
  ❌ البيانات غير محمّلة: مفتاح Firebase غير صالح أو مُبطَل
     1) Firebase Console ← Project Settings ← Service accounts
     …
     ⚠️ هذا وحده يوقف: الربط، والمنيو، والصور، والبوت كله.

════ 4) اتصال واتساب ════
  ❌ غير مرتبط — البوت لا يبدأ لأن البيانات لم تُحمّل
```

اختُبر على حالتين: خادم سليم (كشف نطاق الصور الفارغ فقط)، وخادم بمفتاح Firebase مُبطَل (ربط الأعراض الثلاثة بسببها الواحد).

## v6.5 — 🐛 إصلاح قفل ميت منع الربط + الصور تلقائياً

### السبب الحقيقي لتعذّر الربط — خطأ أدخلتُه في v5.9

الحارس الذي أضفته لمنع الاتصالات المتوازية كان يعلق للأبد:

```js
if (waConnecting) { return; }   // ← لا شيء يعيده إلى false إن لم يفتح السوكيت ولم يُغلق
waConnecting = true;
```

`waConnecting` يعود `false` عند `open` أو `close` أو خطأ. لكن إن أُنشئ السوكيت ثم **صمت** — لا فتح ولا إغلاق، وهو شائع على شبكة متعثّرة — يبقى الحارس مرفوعاً إلى الأبد، فتُرفض **كل** محاولة ربط بعدها:

```
⏭️  محاولة اتصال جارية — تُجوهلت
```

اختُبر: زر «إعادة التشغيل» وطلب كود الربط أنتجا **صفر** اتصالات جديدة.

**الإصلاح:**

- مراقب كل 30 ثانية يفك الحارس إن تجاوز 75 ثانية بلا نتيجة
- `startBaileys({force:true})` للطلبات الصريحة من المستخدم — تتجاوز الحارس دائماً

بعد الإصلاح: كلا الزرّين ينتجان اتصالاً جديداً ✅

### 🐛 خمس بقايا من whatsapp-web.js

`/api/bot/restart` كان يستدعي `scheduleReconnect(1000)` — نسخة **قديمة** من الدالة تشير إلى `client.initialize()` غير الموجود في Baileys. أي أن زر إعادة التشغيل لم يكن يفعل شيئاً منذ الترحيل إلى Baileys.

حُذفت الدالة الميتة، وأُصلحت أربع نقاط أخرى كانت ستنهار عند الاستدعاء: إشعار القروب · قطع الاتصال · رسالة لزبون طلب · رسالة لزبون منتظر.

وغلاف `client.sendMessage` كان يبتلع كل الأخطاء بصمت (`catch(e) {}`) — صار يسجّلها في السجل واللوحة.

### الصور تظهر بلا إعداد يدوي

استضافة Firebase تكون دائماً على `<project_id>.web.app`، ومعرّف المشروع معروف من مفتاح الخدمة. فإن كان «نطاق صور المنيو» فارغاً يُضبط تلقائياً:

```
🖼️  نطاق الصور ضُبط تلقائياً: https://o2bot-b7a51.web.app
```

يمكنك تغييره من الإعدادات إن كانت صورك في مكان آخر — الضبط التلقائي لا يلمس قيمة موجودة.

**خطأ مرافق:** كان الضبط داخل `loadState` بعد نقطة `return` الخاصة بأول تشغيل، فلا يُنفَّذ. نُقل بعد نجاح التحميل — نفس نمط الخطأ الذي أوقع ترحيل المنيو سابقاً.

## v6.6 — الصور: ضبط النطاق بضغطة

المسارات في قاعدة البيانات نسبية بالتصميم:

```
/menu/shawarma/48.jpg
```

وحدها ليست رابطاً. تحتاج نطاقاً قبلها ليصبح:

```
https://o2bot-b7a51.web.app/menu/shawarma/48.jpg
```

حقل **«نطاق صور المنيو»** في الإعدادات هو ما يوفّر هذا النطاق، وكان فارغاً عندك — فظهر المسار الخام بلا صورة.

### ثلاث طبقات لحل هذا نهائياً

**١. ضبط تلقائي عند الإقلاع.** استضافة Firebase دائماً على `<project_id>.web.app`، والمعرّف معروف من مفتاح الخدمة. فإن كان الحقل فارغاً يُضبط وحده:

```
🖼️  نطاق الصور ضُبط تلقائياً: https://o2bot-b7a51.web.app
```

**٢. شريط تنبيه فوق المنيو** إن بقي فارغاً لأي سبب، بزر واحد يضبطه:

```
🖼️ الصور لا تظهر — «نطاق صور المنيو» فارغ
238 صنفاً يحمل مساراً نسبياً مثل /menu/shawarma/48.jpg
        [ ⚡ اضبطه على https://o2bot-b7a51.web.app ]
```

يختفي فور الضبط، ولا يظهر أصلاً إن كانت كل صورك روابط كاملة. اختُبر على الحالات الثلاث.

**٣. الحقل اليدوي** في الإعدادات لمن يستضيف صوره في مكان آخر.

### لماذا نسبية لا كاملة

كتابة الرابط الكامل في كل صنف تعمل، لكنها تربط 238 صنفاً بنطاق واحد داخل قاعدة البيانات. تغيير الاستضافة يعني تعديل 238 سجلاً. المسار النسبي + حقل واحد = تغيير سطر واحد.

الروابط الكاملة تبقى مدعومة وتتجاوز النطاق، فيمكن خلط الاثنين.

## v6.7 — ربط المنيو الإلكتروني للزبائن

الموقع صار يقرأ التوفّر حيّاً من اللوحة. إغلاق صنف من اللوحة أو بأمر `#اغلاق` يخفيه من الموقع خلال دقيقة، بلا نشر ولا تعديل كود.

### نقطة عامة جديدة

```
GET /api/public/menu?branch=gaza
```

بلا تسجيل دخول، مع `Access-Control-Allow-Origin: *` ودعم `OPTIONS`. ترجع الأقسام والأصناف مع `active` والأسعار، مصفّاة بالفرع.

```json
{ "ok": true, "counts": { "total": 116, "active": 106 }, "categories": [...], "items": [...] }
```

تخزين مؤقت 30 ثانية عند الحافة — تغيير الكاشير يظهر بسرعة دون إثقال الخادم.

**خطأ صادفته:** المعالج يقطع الرابط عند `?` في أعلاه، فكان `branch` يُفقد وتُرجع الفروع كلها. صارت تُقرأ من `req.url`.

## v6.8.1 — إعدادات Render + الصور بلا أي إعداد

### فشل البناء على Render

```
ERR_PNPM_NO_LOCKFILE  ·  ERR_PNPM_NO_SCRIPT Missing script: build
```

خدمة البوت كانت مضبوطة بأمر بناء **مشروع الموقع**. البوت يستخدم npm ولا يحتاج خطوة بناء.

**الإعدادات الصحيحة** (Render ← Settings):

```
Build Command:      npm install
Start Command:      node index.js
Health Check Path:  /ping
```

أُضيف `render.yaml` مرجعي و`.node-version`.

**وتثبيت إصدار Node:** كان `engines: >=18.0.0` مفتوحاً فاختار Render **Node 26.8.1** — إصدار حديث جداً وBaileys لم يُختبر عليه. ثُبّت على `22.x` (LTS).

### الصور تعمل بلا نطاق ولا إعداد

البوت صار يقدّم صوره بنفسه:

```
GET /menu/shawarma/48.jpg  →  200 image/jpeg
```

من مجلد `public/menu` الموجود في المشروع أصلاً، بترويسة `Cache-Control: immutable` فتُحمَّل مرة واحدة.

النتيجة: المسار `/menu/…` يعمل على نطاق اللوحة نفسه — **بلا Firebase Hosting، بلا نطاق، بلا CORS، بلا إعداد**.

عند الإقلاع يُطبع:

```
🖼️  137 صورة تُخدَم من /menu/ مباشرة
```

**الحماية:** الخروج من المجلد (`/menu/../../index.js`) يرجع 404، والامتدادات غير الصورية ترجع 404. اختُبرت أربع حالات.

**حقل «نطاق صور المنيو» يبقى** لمن يريد شبكة توزيع خارجية — إن ضُبط يتجاوز الخدمة المحلية. والترتيب صار:

| مصدر الصورة | الأولوية |
|---|---|
| رابط كامل على الصنف | الأعلى |
| `/api/img/…` (مرفوعة من الجهاز) | نفس الخادم |
| نطاق خارجي مضبوط | إن وُجد |
| **البوت نفسه `/menu/…`** | الافتراضي الجديد |
| رمز القسم | عند فشل كل ما سبق |

الشريط التحذيري لم يعد يظهر لمجرد فراغ النطاق — صار يظهر فقط إن **فشل تحميل صورة فعلياً**، ويُختبر ذلك بتحميل صورة حقيقية عند فتح المنيو.

## v6.8.2 — البوت يصمد أمام أوامر البناء الخاطئة

`npm ci && npm run build` (أمر الموقع) على خدمة البوت كان يفشل بسببين: لا `package-lock.json` ولا سكربت `build`.

**أُضيف الاثنان:**

- **`package-lock.json`** (265 حزمة، lockfileVersion 3) — ممارسة صحيحة أصلاً: تثبّت إصدارات التبعيات فلا يتغيّر البناء بين مرة وأخرى، ويجعل `npm ci` يعمل.
- **سكربت `build`** لا يفعل شيئاً سوى طباعة ملاحظة وينجح.

اختُبر الأمران على نسخة نظيفة: `npm ci && npm run build` ✅ و`npm install` ✅.

هذا لا يغني عن ضبط الإعدادات صحيحاً، لكنه يمنع فشل نشر بسبب خطأ في حقل واحد.
